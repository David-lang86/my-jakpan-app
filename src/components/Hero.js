import React from 'react';
import './Hero.css';

const Hero = () => {
  return (
    <section className="hero" id="home">
      {/* No content - just the background image */}
    </section>
  );
};

export default Hero;